diff --git a/archiso/airootfs/etc/skel/.config/picom.conf b/archiso/airootfs/etc/skel/.config/picom.conf
index 234f310..2d1cc57 100644
--- a/archiso/airootfs/etc/skel/.config/picom.conf
+++ b/archiso/airootfs/etc/skel/.config/picom.conf
@@ -24,6 +24,16 @@ fade-in-step = 0.05;
 fade-out-step = 0.05;
 fade-delta = 6;
 
+# Blur (glassmorphism / vidrio esmerilado) — faltaba, por eso XFCE no tenía glass
+blur-method = "dual_kawase";
+blur-strength = 5;
+blur-background = true;
+blur-background-exclude = [
+    "window_type = 'dock'",
+    "window_type = 'desktop'",
+    "_GTK_FRAME_EXTENTS@:c"
+];
+
 # Corners & Transparency
 corner-radius = 10;
 rounded-corners-exclude = [
